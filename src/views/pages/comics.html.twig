{% extends 'layout.html.twig' %}
{% block head %}
    {{parent()}}
{% endblock %}
{% block content %}
	{{ parent() }}
    <br />
      <div class="row">
        <div class="col-xl-12">
            <h2 class="card-title">Comics</h2>
        </div>
      </div>
      <div class="row">
        {% for slug, comic in comics %}
          {% include "partials/comic_card.html.twig" with {'columns':6} %}
        {% endfor %}
      </div>
{% endblock %}
